<%= simple_form_for(work_shop) do |f| %>
  <% if work_shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_shop.errors.count, "error") %> prohibited this work_shop from being saved:</h2>

      <ul>
      <% work_shop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name, label: 'Workshop title' %>
  </div>

  <div class="field">
    <%= f.input :description, label: 'Description' %>
  </div>

  <div class="field">
    <%= f.input :location_id, label: 'Location' %>
  </div>

  <div class="field">
    <%= f.input :drop_in, label: 'Dropins allowed?' %>
  </div>

  <div class="field">
    <%= f.input :leader_id, label: 'Workshop Leader' %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
